const LikeService = require('../../services/like_service');
const User = reuqire('../../models/mongo/user')
const {expect} = require('chai');

describe('likeService#likeTopic', async () => {

    it('should add 10 points to user when topic liked', async ()=>{
        // stub, spy, mock
    })

})